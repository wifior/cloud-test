server:
  port: 8081
spring:
  application:
    name: eureka-client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/
  instance:
    prefer-ip-address: true
    instance-id: eureka-client-id